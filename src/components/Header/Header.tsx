import './Header.scss';

const Header: React.FC<{}> = () => {
  return (
    <header className="header">
      <h1 className="header__title">Стриминг с использованием socket.io</h1>
    </header>
  );
};

export default Header;
